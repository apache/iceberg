***************
*** 115,123 ****
      List<Filter> pushed = Lists.newArrayListWithExpectedSize(filters.length);
  
      for (Filter filter : filters) {
-       Expression expr = null;
        try {
-         expr = SparkFilters.convert(filter);
        } catch (IllegalArgumentException e) {
          // converting to Iceberg Expression failed, so this expression cannot be pushed down
          LOG.info(
--- 115,123 ----
      List<Filter> pushed = Lists.newArrayListWithExpectedSize(filters.length);
  
      for (Filter filter : filters) {
+       Tuple<Boolean, Expression> exprTuple = null;
        try {
+         exprTuple = SparkFilters.convert(filter, this.schema);
        } catch (IllegalArgumentException e) {
          // converting to Iceberg Expression failed, so this expression cannot be pushed down
          LOG.info(
***************
*** 126,132 ****
              e.getMessage());
        }
  
-       if (expr != null) {
          try {
            Binder.bind(schema.asStruct(), expr, caseSensitive);
            expressions.add(expr);
--- 126,133 ----
              e.getMessage());
        }
  
+       if (exprTuple != null) {
+         Expression expr = exprTuple.getElement2();
          try {
            Binder.bind(schema.asStruct(), expr, caseSensitive);
            expressions.add(expr);
***************
*** 221,231 ****
  
      Schema expectedSchema = schemaWithMetadataColumns();
  
      TableScan scan =
          table
              .newScan()
              .caseSensitive(caseSensitive)
              .filter(filterExpression())
              .project(expectedSchema);
  
      if (snapshotId != null) {
--- 222,234 ----
  
      Schema expectedSchema = schemaWithMetadataColumns();
  
+     // TODO: Asif: Check if we can figure out apriori we would need stats or not
      TableScan scan =
          table
              .newScan()
              .caseSensitive(caseSensitive)
              .filter(filterExpression())
+             .includeColumnStats()
              .project(expectedSchema);
  
      if (snapshotId != null) {
